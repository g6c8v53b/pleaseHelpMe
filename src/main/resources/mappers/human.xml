<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--<mapper namespace="命名空间dao接口">-->
<mapper namespace="com.qf.tx.mapper.HumanMapper">

<insert id="insert">
    INSERT INTO human VALUE (#{human.idCard},#{human.name},#{human.age},#{human.born})
    </insert>
</mapper>